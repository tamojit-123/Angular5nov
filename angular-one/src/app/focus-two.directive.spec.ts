import { FocusTwoDirective } from './focus-two.directive';

describe('FocusTwoDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusTwoDirective();
    expect(directive).toBeTruthy();
  });
});
